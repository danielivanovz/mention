---
title: Accessibility
description: Interaction behavior, automated checks, and manual verification limits.
---

The editor keeps DOM focus while suggestions are navigated. The popup uses `role="listbox"`; options use `role="option"` and `aria-selected`. The host's `aria-activedescendant` points to the highlighted option, and `aria-controls` points to the mounted listbox.

Name the input with a label or `aria-label`, and give the listbox an accessible name through `Mention.Popover`.

## Keyboard behavior

| Key | Behavior while suggestions are open |
| --- | --- |
| ArrowDown / ArrowUp | Move the highlight and reveal it in the scrolling list. |
| Enter / Tab | Commit the current result if the selection is unchanged. |
| Escape | Dismiss without editing the text. |
| IME confirmation | Leave composition to the host; do not commit a suggestion. |

Blur and outside pointer interaction dismiss the list. Consumer key handlers can prevent Mention from handling a key.

## Known conformance issue

The textarea implementation currently uses a persistent `role="combobox"`. The [HTML-ARIA textarea entry](https://www.w3.org/TR/html-aria/#el-textarea) does not permit this role. ARIA's browser processing rules do not override HTML author conformance requirements.

The existing role behavior is retained pending a focused accessibility design and manual verification. It should not be described as unconditionally conformant.

A portaled listbox can also trigger axe's `region` best-practice rule because it is outside a landmark. Use `container={null}` or an appropriate landmark container when needed.

## Verification

Browser tests check focus, keyboard behavior, attribute relationships, and axe's automated rules. A separate test records the known `aria-allowed-role` and `region` findings so additional failures remain visible.

The original prototype had user-reported assistive-technology testing. That result does not validate the current implementation or this structural revision. The current library still needs manual checks with NVDA, JAWS, VoiceOver, and TalkBack, plus real OS IMEs. Synthetic composition tests cover event handling, not real candidate-window behavior.

Test with the actual application, including dialogs, portals, scrolling containers, input labels, and custom option content. For TalkBack swipe order, an in-place popup is available through `container={null}`.

Default styles include forced-colors rules. Custom styling must preserve a visible highlighted state.
