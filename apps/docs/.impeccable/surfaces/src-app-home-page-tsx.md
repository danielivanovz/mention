---
version: 1
slug: "src-app-home-page-tsx"
primary_target: "src/app/(home)/page.tsx"
related_targets: ["src/app/layout.tsx","src/app/global.css","src/components/mention-demo.tsx","src/app/docs/layout.tsx","src/app/docs/[[...slug]]/page.tsx"]
---

# Mention website replacement

Landing mode: Persuade. Docs and coding-agent guide: Read. Scope is the complete website in apps/docs; library runtime behavior stays authoritative.

The developer should try a real mention, choose an editing host, and reach a working integration. Coding agents receive the same MDX truth through direct focused Markdown links. No customer claims or unverified adapters.

Direction: Type foundry proof, grounded candidate 4, seed 51aacbab. Approved composition: `.impeccable/mocks/ink-block.png`; approval and detailed geometry in its JSON sidecar. The user explicitly stopped further comp work and requested implementation. The delegated product representative inspected and approved the completed comp. No unresolved design decisions.

First viewport: small navy navigation; almost width-filling heavy lowercase mention in pale blue; descriptor row; broad pink editing specimen; sheet install/action band; blue editor-choice section. The name is about seven times the descriptor size, and the input is a broad working field rather than a chat card. Rectangular controls use 6px corners, 1px identifying borders, no card elevation, and a soft popup shadow only. Heavy normal-width grotesk display; ordinary Archivo for reading and controls, JetBrains Mono for real source. Dark mode preserves clear color regions.

Implementation inventory: `.impeccable/assets/manifest.json`, independently reviewed by the asset producer. All text, geometry, controls and editing are semantic HTML/CSS; icons are Lucide SVG. No raster production assets. Never rasterize the editable specimen. Comp defects corrected: popup follows real caret, options match current query, no initial autofocus, no generated surface shading, no implied universal editor compatibility.

Docs inherit palette, typography and controls in a restrained Read treatment with Fumadocs search, responsive navigation and TOC. Explicit Markdown links supplement copy-page. Source links are shown only when a matching repository revision is configured; unpushed local pages never invent a public source URL. Direct Markdown URLs are used instead of content negotiation on this Next version.

Contract is preserved in the first body element's inert template as an emitted HTML comment; React does not natively render a standalone comment child. Do not introduce runtime DOM mutation solely for a design annotation.

The consistency pass gives landing and docs one persistent ink header with shared logo geometry, search, theme, and route indication. Mobile docs combine page browsing and section navigation in one bar. Copy actions share truthful success/failure behavior; visual success never shifts the install band. These changes refine the approved direction without replacing its composition.
